package org.neteinstein.snap2sheet.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Brand tokens lifted directly from the Fatura design (Main/Home/Review/... .dc.html): a cool
 * neutral ground with a single blue accent, plus status colors for the sync/review/failed chips.
 */
object FaturaColors {
    val Accent = Color(0xFF2E5AAC)
    val AccentSoft = Color(0xFFEAF0FB)

    val Ink = Color(0xFF12151B)
    val Muted = Color(0xFF5B6472)
    val MutedStrong = Color(0xFF8B93A1)
    val Subtle = Color(0xFF98A0AC)

    val Background = Color(0xFFF3F4F6)
    val Surface = Color(0xFFFFFFFF)
    val SurfaceMuted = Color(0xFFF8F9FB)
    val Border = Color(0xFFE4E7ED)
    val BorderStrong = Color(0xFFC7CBD3)
    val Divider = Color(0xFFEEF0F3)

    val Success = Color(0xFF1F8A5F)
    val SuccessSoft = Color(0xFFE7F5EE)
    val Warning = Color(0xFFB7791F)
    val WarningSoft = Color(0xFFFBF0DF)
    val Danger = Color(0xFFB42318)
    val DangerSoft = Color(0xFFFDEAEA)
}
